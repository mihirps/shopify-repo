{%- if product.handle != blank -%}
<a class="wishlist-btn" 
   href="javascript: void(0)" 
   button-wishlist 
   data-product-handle="{{ product.handle }}" 
   data-state="add"
   aria-label="{{ settings.wishlist_title }}">
  <i class="pe-7s-like wishlist-icon"></i>
</a>
{%- endif -%}